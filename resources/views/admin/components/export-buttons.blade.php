<div style="display: flex; align-items: center; gap: 8px; flex-wrap: wrap;">
    <a href="{{ route('admin.exports.pdf', $dataset) }}" class="btn btn-ghost">PDF</a>
    <a href="{{ route('admin.exports.excel', $dataset) }}" class="btn btn-ghost">Excel</a>
</div>
